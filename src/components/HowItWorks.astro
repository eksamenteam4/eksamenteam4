<!-- -------------HTML START------------- -->
<section class="how-section">
  <div class="how-left">
    <div class="how-card-photo">
      <img src="/img/card.webp" alt="Rødt Dankort" />
    </div>

    <div class="how-nature">
      <h3 class="heading-3">Dit kort.<br />Gør en forskel.</h3>
    </div>
  </div>

  <div class="how-text-box">
    <h2 class="heading-2">
      Du swiper allerede.
      <span>Vælg kortet, der giver mening.</span>
    </h2>

    <p class="body-1">
      Mange unge har allerede en bank, men ikke nødvendigvis Dankort. Heldigvis kan du nemt bestille
      det hos os, med bare <strong>3 nemme trin.</strong>
    </p>

    <a class="button-red button-large" href="/form/">
      Bestil dit Dankort her
      <span>→</span>
    </a>
  </div>
</section>
<!-- -------------HTML SLUT------------- -->

<!-- -------------CSS START------------- -->
<style>
  .how-section {
    display: flex;
    flex-direction: column;
    gap: 22px;
  }

  .how-left {
    display: contents;
  }

  .how-card-photo,
  .how-nature,
  .how-text-box {
    border-radius: var(--radius-card);
    overflow: hidden;
  }

  .how-card-photo {
    order: 1;
    aspect-ratio: 1.45 / 1;
    background-color: var(--color-image-light);
  }

  .how-card-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .how-nature {
    order: 3;
    min-height: 104px;
    padding: 40px;
    background-image:
      linear-gradient(90deg, #00000099, #00000033),
      url("/img/grass.webp");
    background-size: cover;
    background-position: center;
    color: white;
    display: flex;
    align-items: center;
    
  }

  .how-nature h3 {
    margin-bottom: 0;
    font-size: 22px;
    line-height: 26px;
  }

  .how-text-box {
    order: 2;
    min-height: 450px;
    padding: 40px 36px 34px;
    background-color: var(--color-card-light);
    display: flex;
    flex-direction: column;
  }

  .how-text-box h2 {
    max-width: 300px;
    margin-bottom: 34px;
    font-size: 40px;
    line-height: 44px;
  }

  .how-text-box h2 span {
    color: var(--color-red);
    display: block;
  }

  .how-text-box p {
    max-width: 285px;
    margin-bottom: 0;
    font-size: 14px;
    line-height: 19px;
  }

  .how-text-box p strong {
    display: block;
  }

  .how-text-box .button-red {
    width: 260px;
    margin-top: 58px;
    padding-top: 16px;
    padding-bottom: 16px;
    align-self: flex-start;
  }
  .body-1 {
   
  }

  @media (min-width: 701px) {
    .how-section {
      display: grid;
      grid-template-columns: 1fr 0.58fr;
      gap: 28px;
      align-items: stretch;
    }

    .how-left {
      display: grid;
      grid-template-rows: 430px 170px;
      gap: 28px;
    }

    .how-card-photo {
      order: 0;
      aspect-ratio: auto;
    }

    .how-text-box {
      order: 0;
      padding: 74px 48px 52px;
    }

    .how-nature {
      order: 0;
      padding: 34px;
    }

    .how-nature h3 {
      font-size: var(--h3-size);
      line-height: var(--h3-line-height);
    }

    .how-text-box h2 {
      max-width: none;
      margin-bottom: 28px;
      font-size: var(--h2-size);
      line-height: var(--h2-line-height);
    }

    .how-text-box p {
      max-width: none;
      font-size: var(--body-1-size);
      line-height: var(--body-1-line-height);
    }

    .how-text-box p strong {
      display: inline;
    }

    .how-text-box .button-red {
      width: auto;
      margin-top: auto;
      padding-top: var(--button-padding-y);
      padding-bottom: var(--button-padding-y);
    }
  }
</style>
<!-- -------------CSS SLUT------------- -->
